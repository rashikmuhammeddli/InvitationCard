@page
@model IndexModel

<div class="scene">

    <!-- 🕌 INTRO -->
    <div id="intro" class="intro">
        <div class="intro-content">

            <h2 class="arabic">
                بِسْمِ ٱللَّٰهِ ٱللَّٰهُ ٱلرَّحْمَٰنِ ٱلرَّحِيمِ
            </h2>

            <p class="intro-small">Wedding Invitation</p>
            <button class="enter-btn" onclick="enterInvitation()">
                Tap to Open
            </button>

        </div>
    </div>

    <!-- 💌 INVITATION CARD -->
    <div id="invite" class="invite">
        <div class="pdf-page">
        <div class="card">

            <!-- TEXT (UNCHANGED CONTENT) -->
            <p class="tagline">Together With Their Families</p>

            <div class="divider"></div>

            <p class="arabic-text">
                بِسْمِ ٱللَّٰهِ ٱللَّٰهُ ٱلرَّحْمَٰنِ ٱلرَّحِيمِ
            </p>

            <p class="quote">
                With the blessings of Almighty Allah, we warmly invite you
                to join the wedding ceremony of
            </p>

            <h1 class="names">Rashik <span>&</span> Nidha</h1>

            <p class="quote">
                and celebrate this beautiful union with family and friends.
            </p>

            <div class="event-text">

                <h3>Wedding Ceremony</h3>

                <p class="event-date">Sunday, 15 November 2026</p>
                <p>10:00 AM onwards</p> 

                <p class="venue">
                    Anoos Auditorium
                </p>
                <p class="place">
                    Arangotukara
                </p>

            </div>

            <a class="map-btn"
               href="https://www.google.com/maps/search/Anoos+Auditorium+Arangotukara+Thrissur"
               target="_blank">
                📍 View Location
            </a>

            <div class="countdown">
                <div><span id="days"></span><small>Days</small></div>
                <div><span id="hours"></span><small>Hours</small></div>
                <div><span id="minutes"></span><small>Minutes</small></div>
                <div><span id="seconds"></span><small>Seconds</small></div>
            </div>

            <p class="dua">
                "May Allah bless this marriage with love, mercy and barakah"
            </p>

            <p class="qtn">
                will you attend?
            </p>
            <div class="buttons">
                <button class="yes-btn" onclick="showYes()"> Yes, Insha Allahh. </button>
                <button class="no-btn" onclick="showNo()">Unfortunately, I can't come..</button>
            </div>

        </div>
        </div>

    </div>

    <!-- 🎉 CELEBRATION -->
    <div id="celebration" class="celebration">

        <canvas id="fireworksCanvas"></canvas>

        <div class="celebration-overlay">

            <div class="celebration-card">

                <h1>🎉 Alhamdulillah</h1>
                <p>We look forward to celebrating with you 🤍</p>

                <button class="back-btn" onclick="goBack()">
                    ← Back to Invitation
                </button>

            </div>

        </div>

    </div>


    <!-- 🎉 Click No -->
    <div id="clckNo" class="clckNo">

        <div class="clckNo-overlay">

            <div class="clckNo-card">

                <p>We will miss you 🤍</p>

                <button class="back-btn" onclick="goBack()">
                    ← Back to Invitation
                </button>

            </div>

        </div>

    </div>

</div>

<audio id="bgMusic" loop>
    <source src="/music/wedding.mp3" type="audio/mpeg">
</audio>

<!-- ================= CSS ================= -->
<style>
    /* GLOBAL */
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: Georgia, serif;
    }

    body, html {
        overflow-x: hidden; /* Prevent horizontal wobble */
        background: #f4eee6;
    }

    /* Update these in your <style> section */
    .scene {
        width: 100%;
        height: 100vh; /* Lock to screen height */
        overflow: hidden; /* Disable scrolling */
        display: flex;
        justify-content: center;
        align-items: center; /* Center vertically for a 'one-page' feel */
    }

    .pdf-page {
        width: 794px;
        min-height: auto; /* Let content determine height */
        max-height: 1123px;
        margin: 0 !important;
        background: white;
        padding: 40px 50px; /* Reduced padding slightly to save space */
        box-shadow: 0 20px 60px rgba(0,0,0,0.15);
        transform-origin: center center; /* Scale from center for balanced fit */
    }

    .invite {
        height: 100vh;
        width: 100vw;
    }

    /* MOBILE SCALE LOGIC */
    @@media screen and (max-width: 768px) {
        .pdf-page {
            width: 794px !important;
            min-width: 794px !important;
            min-height: 1123px !important;
            margin: 0 auto !important; /* Center it horizontally */
            padding: 60px 50px !important;
            /* Change this to top center to prevent it from floating in the middle */
            transform-origin: top center;
            flex-shrink: 0;
            box-shadow: 0 10px 40px rgba(0,0,0,0.1);
        }

        .scene {
            /* Ensure the container allows scrolling if the scaled card is tall */
            align-items: flex-start !important;
            overflow-y: auto !important;
            padding-top: 10px;
        }
    }
/* 
    .scene {
        min-height: 100vh;
        height: auto;
        display: block;
        padding: 40px 0;
    } */

    /* INTRO */
    .intro {
        position: absolute;
        inset: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        background: #f4eee6;
        z-index: 20;
    }

    .intro-content {
        text-align: center;
    }

    .arabic {
        font-size: 30px;
        color: #c7a27c;
        margin-bottom: 20px;
        animation: glow 2.5s ease-in-out infinite alternate;
    }

    @@keyframes glow {
        from {
            text-shadow: 0 0 5px rgba(199,162,124,0.4);
        }

        to {
            text-shadow: 0 0 20px rgba(199,162,124,0.9);
        }
    }

    .enter-btn {
        background: linear-gradient(135deg,#c7a27c,#a68a64);
        border: none;
        padding: 14px 30px;
        border-radius: 30px;
        color: white;
        cursor: pointer;
    }

    /* PDF PAGE */
    /* .pdf-page {
        width: 794px;
        min-height: 1123px;
        margin: 40px auto;
        background: white;
        padding: 60px 50px;
        box-shadow: 0 20px 60px rgba(0,0,0,0.15);
    } */
    

    /* Make card fit inside page */
    .card {
        width: 100%;
        max-width: 100%;
        min-height: auto;
        box-shadow: none;
        border: none;
        padding: 0;
        text-align: center;
        justify-content: center;
        align-items: center;
    }

    /* INVITE */
    .invite {
        display: none;
        justify-content: center;
        align-items: center;
        width: 100%;
        height: 100vh;
    }

    /* CARD */
    /* .card {
        background: rgba(255,255,255,0.97);
        min-height: 70vh;
        padding: 90px 80px;
        border-radius: 35px;
        width: 95%;
        max-width: 900px;
        box-shadow: 0 40px 120px rgba(0,0,0,0.15), inset 0 0 0 1px rgba(199,162,124,0.3);
        border: 1px solid rgba(199,162,124,0.35);
        text-align: center;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    } */

    /* ================= MODERN TEXT STYLE ================= */

    .tagline {
        letter-spacing: 3px;
        font-size: 13px;
        color: #a68a64;
        text-transform: uppercase;
        margin-bottom: 10px;
    }

    .divider {
        width: 120px;
        height: 2px;
        background: linear-gradient(to right,transparent,#c7a27c,transparent);
        margin: 20px auto;
    }

    .arabic-text {
        font-size: 22px;
        color: #c7a27c;
        margin: 18px 0;
        text-shadow: 0 0 10px rgba(199,162,124,0.15);
    }

    .quote {
        font-size: 15.5px;
        color: #555;
        line-height: 1.9;
        max-width: 650px;
        margin: 12px auto;
    }

    .names {
        font-size: 70px;
        font-family: "Mistral", cursive;
        font-weight: 400;
        color: #2c2c2c;
        letter-spacing: 1px;
        margin: 25px 0;
    }

        .names span {
            color: #c7a27c;
        }

    .event-text {
        text-align: center;
        margin: 25px 0;
    }

        .event-text h3 {
            font-size: 18px;
            font-weight: 600;
            color: #a68a64;
            letter-spacing: 1px;
            margin-bottom: 10px;
        }

        .event-text p {
            font-size: 15px;
            color: #444;
            margin: 4px 0;
        }

        .event-text .event-date {
            font-size: 30px; /* increase as you like */
            font-weight: 600;
            font-family: "Mistral", cursive;
        }

        .event-text .venue {
            margin-top: 30px;
            font-size: 20px; /* increase as you like */
            font-weight: 600;
            font-style: italic;
            color: #666;
            line-height: 1.6;
        }

        .event-text .place {
            margin-top: 7px;
            font-size: 17px; /* increase as you like */
            font-style: italic;
            color: #666;
            line-height: 1.3;
        }

    .map-btn {
        display: inline-block;
        padding: 12px 50px;
        background: linear-gradient(135deg,#c7a27c,#a68a64);
        color: white;
        border-radius: 25px;
        text-decoration: none;
        margin: 15px 0;
    }

    .countdown {
        display: flex;
        gap: 15px;
        margin-top: 20px;
    }

        .countdown div {
            background: white;
            padding: 10px 15px;
            border-radius: 12px;
            box-shadow: 0 10px 20px rgba(0,0,0,0.05);
        }

        .countdown span {
            font-size: 24px;
            font-weight: 600;
        }

        .countdown small {
            font-size: 11px;
            color: #999;
            display: block;
        }

    .dua {
        margin-top: 25px;
        font-size: 14px;
        font-style: italic;
        color: #888;
    }

    .qtn {
        margin-top: 25px;
        font-size: 14px;
        font-style: italic;
        color: #888;
    }

    .yes-btn, .no-btn {
/*         padding: 14px 30px;
 */        border-radius: 30px;
           cursor: pointer;
    }

    .yes-btn {
        background: linear-gradient(135deg,#c7a27c,#a68a64);
        padding: 17px 80px;
        border: none;
        color: white;
        margin: 8px;
    }

    .no-btn {
        border: 1px solid #c7a27c;
        background: none;
        padding: 14px 30px;
    }

    /* CELEBRATION BACKDROP (DO NOT HIDE DESIGN COMPLETELY) */
    .celebration {
        position: fixed;
        inset: 0;
        display: none;
        z-index: 9999;
        overflow: hidden;
    }

    .clckNo {
        position: fixed;
        inset: 0;
        display: none;
        z-index: 9999;
        overflow: hidden;
    }

    .clckNo-overlay {
        position: fixed;
        inset: 0;
        background: rgba(0,0,0,0.55);
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .clckNo-card {
        background: rgba(255,255,255,0.95);
        font-size: 22px; /* increase this value */
        padding: 40px;
        border-radius: 20px;
        text-align: center;
        box-shadow: 0 20px 60px rgba(0,0,0,0.2);
    }

    /* FIREWORKS BEHIND EVERYTHING */
    #fireworksCanvas {
        position: fixed;
        inset: 0;
        width: 100%;
        height: 100%;
        z-index: 9998; /* 🔥 bring it to front */
        pointer-events: none;
    }

    /* DARK OVERLAY ONLY */
    .celebration-overlay {
        position: fixed;
        inset: 0;
        background: rgba(0,0,0,0.55);
        display: flex;
        justify-content: center;
        align-items: center;
        z-index: 2;
    }

    /* YOUR CARD STAYS BEAUTIFUL */
    .celebration-card {
        background: rgba(255,255,255,0.95);
        padding: 40px;
        border-radius: 20px;
        text-align: center;
        box-shadow: 0 20px 60px rgba(0,0,0,0.2);
        animation: pop 0.6s ease;
    }

    /* SMALL ENTRY ANIMATION */
    @@keyframes pop {
        from

    {
        transform: scale(0.7);
        opacity: 0;
    }

    to {
        transform: scale(1);
        opacity: 1;
    }

    }

    /* BUTTON */
    .back-btn {
        margin-top: 20px;
        padding: 12px 25px;
        border: none;
        border-radius: 25px;
        background: #c7a27c;
        color: white;
        cursor: pointer;
    }

</style>

<!-- ================= SCRIPT ================= -->
<script>

      let canvas, ctx;
    let particles = [];
    let fireworksInterval;
    let animationRunning = false;

    function enterInvitation(){
        document.getElementById("intro").style.display="none";
        document.getElementById("invite").style.display="flex";
        startCountdown();
        document.getElementById("bgMusic").play().catch(()=>{});
    }

    function showYes(){
        document.getElementById("celebration").style.display="flex";
        startFireworks();
    }

    function showNo(){
        document.getElementById("clckNo").style.display="flex";
    }

        function goBack(){
        document.getElementById("celebration").style.display="none";
        document.getElementById("clckNo").style.display="none"; 

        clearInterval(fireworksInterval);
        animationRunning = false;
        particles = [];

        if (ctx && canvas) {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
        }
    }

    /* COUNTDOWN */
    function startCountdown(){
        let date=new Date("Nov 15, 2026 10:00:00").getTime();

        setInterval(()=>{
            let now=new Date().getTime();
            let diff=date-now;

            document.getElementById("days").innerHTML=Math.floor(diff/(1000*60*60*24));
            document.getElementById("hours").innerHTML=Math.floor((diff%(1000*60*60*24))/(1000*60*60));
            document.getElementById("minutes").innerHTML=Math.floor((diff%(1000*60*60))/(1000*60));
            document.getElementById("seconds").innerHTML=Math.floor((diff%(1000*60))/1000);

        },1000);
    }

    /* FIREWORKS START */
    function startFireworks(){

        canvas = document.getElementById("fireworksCanvas");
        ctx = canvas.getContext("2d");

        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        animationRunning = true;

        fireworksInterval = setInterval(() => {
            launchFirework();
        }, 500);

        animate();
    }

    /* CREATE BURST */
    function launchFirework(){

        let x = Math.random() * canvas.width;
        let y = Math.random() * canvas.height / 2;

        for (let i = 0; i < 60; i++) {
            particles.push({
                x,
                y,
                angle: Math.random() * 2 * Math.PI,
                speed: Math.random() * 6 + 2,
                radius: Math.random() * 3 + 1.5,
                alpha: 1,
                color: ["#ffd700","#ff4d6d","#6bcB77","#4d96ff","#ffffff"][Math.floor(Math.random()*5)]
            });
        }
    }

    /* LOOP */
    function animate(){

        if (!animationRunning) return;

        ctx.clearRect(0, 0, canvas.width, canvas.height);

        particles.forEach((p, i) => {

            p.x += Math.cos(p.angle) * p.speed;
            p.y += Math.sin(p.angle) * p.speed;
            p.alpha -= 0.012;

            ctx.globalAlpha = p.alpha;
            ctx.fillStyle = p.color;

            ctx.beginPath();
            ctx.arc(p.x, p.y, p.radius, 0, Math.PI * 2);
            ctx.fill();

            if (p.alpha <= 0) particles.splice(i, 1);
        });

        requestAnimationFrame(animate);
    }

    function fitToScreen() {
        const card = document.querySelector('.pdf-page');
        if (!card) return;

        // Reset scale to measure actual size
        card.style.transform = 'scale(1)';

        const cardWidth = card.offsetWidth;
        const cardHeight = card.offsetHeight;
        const windowWidth = window.innerWidth;
        const windowHeight = window.innerHeight;

        // Calculate scale for width and height
        // 0.9 provides a small margin so it doesn't touch the edges
        const scaleX = (windowWidth * 0.9) / cardWidth;
        const scaleY = (windowHeight * 0.9) / cardHeight;

        // Use the SMALLEST scale to ensure it fits both ways
        const finalScale = Math.min(scaleX, scaleY);

        card.style.transform = `scale(${finalScale})`;
    }

    // Update your enterInvitation to call this
    function enterInvitation(){
        document.getElementById("intro").style.display="none";
        document.getElementById("invite").style.display="flex";

        // Small timeout ensures the browser has rendered the 'invite' div before measuring
        setTimeout(fitToScreen, 10);

        startCountdown();
        document.getElementById("bgMusic").play().catch(()=>{});
    }

    window.addEventListener('resize', fitToScreen);

    // Also call it if the screen rotates
    window.addEventListener('resize', fitToScreen);

</script>